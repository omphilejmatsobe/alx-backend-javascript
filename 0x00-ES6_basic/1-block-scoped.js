export default function taskBlock(trueOrFalse) {

  
  if (trueOrFalse) {
    // eslint-disable-line no-unused-vars
    var task = true;
    // eslint-disable-line no-unused-vars
    var task2 = false; 
  }

  var task = false;
  var task2 = true;

  return [task, task2];
}
